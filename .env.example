# Database Configuration
# CRITICAL: Use absolute path to prevent multiple database files
DATABASE_URL=sqlite:////workspaces/agent-kanban/agent_kanban.db

# API Configuration (DevContainer ports)
API_HOST=0.0.0.0
API_PORT=18000

# CORS Configuration
CORS_ORIGINS=["http://localhost:15173", "http://localhost:3000", "http://localhost:5173"]

# WebSocket Configuration
WS_PING_INTERVAL=30
WS_PING_TIMEOUT=10
WS_URL=ws://localhost:18000/ws

# MCP Server Configuration
MCP_SERVER_NAME=agent-kanban-mcp
MCP_SERVER_VERSION=1.0.0
MCP_SERVER_PORT=13000  # Reserved for standalone MCP if needed

# Frontend Configuration
FRONTEND_PORT=15173
VITE_API_URL=http://localhost:18000
